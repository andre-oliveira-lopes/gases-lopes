----------> commit: 
Pedidos Especiais: Adicionei um novo comando no package.json, o 'npm run gerar-estrutura' que gera a extrutura do meu projeto atual e me da em um arquivo .txt chamado 'estrutura_do_projeto.txt', pra isso baixei outro modulo para o node 'tree-cli'. para um visual em imagem desse meu projeto, fiz a mesma ideia em plantUML 'estrutura-projeto.puml'. Adicionei barra de rolagem dentro do modal, tirei ela da pagina. 



----------> Emojis
✅❌🔍🗑️✏️🚀➕⏳--📋🔄 

 
----------> feature" (funcionalidade)
* Adicionar uma nova aba no trecho atividades, com o nome "Troca"
    - essa aba deve apresentar uma forma facil de fazer troca entre cilindros 
    de clientes e da empresa. 
* Adicionar uma nova aba no trecho atividades, com o nome "Lembretes"
    - essa aba deve ser uma anotação de lembretes de coisas corriqueiras que 
    não tem tanta importancia, como por exemplo, enviamos um cilindro para são 
    joão dos patos e o cara ficou de enviar o clindro pela van depois. Isso 
    precisa ser lembrado.
* Adicionar uma nova aba no trecho informações, com o nome "Estoque"
    - essa aba deve apresentar a quantidade dos tipos de gases que temos para 
    vender: Oxigênio, Acetileno, Nitrogênio, Argônio, Mistura, CO², Ar Medicinal.

----------> bug (Problemas)
* lembrete: aviso de segurança do Electron alertando que seu app está configurado de forma que pode ser vulnerável a certos tipos de ataques.
* Os cards ficam em formato de grade, mas não estão padrões. 
* Melhorar o visual do modal pra não pegar a tela toda. 
* Possibilidade de adicionar mais de um produto no cadastro. 
* Seria necessário criar um arquivo PDF simples para que eu possa fazer dawnloads.  

Adicionar um campo no banco de dados:
* Ao apagar no botão da lixeira, ele só apaga no front-end. Ainda fica salvo no banco. O botão vai mortificar a coluna “Oculto” do banco, como padrão quando cria um registro vai ser SIM. Quando excluir, vai mudar para NÃO. 
* Adicionar o campo de qual fornecedor levou o pedido especial. 
* e quando eu tiver guardando um pedido especial de uma empresa?

=======================================================================
Melhorar o modal pra deixar mais bonito
colocar os campos que agora eu posso faze busca. 

Sobre documentação e visualização com PlantUML
Vamos criar um arquivo de documentação: docs/banco-erd.puml.
Nesse arquivo, descreveremos as tabelas (como pedidos_especiais) em formato texto com PlantUML.
Esse arquivo servirá como “mapa” do banco: colunas, tipos e relacionamentos.
Vamos usar uma extensão de PlantUML no VS Code para visualizar o diagrama diretamente no editor.
Com o tempo, manteremos o diagrama atualizado conforme o banco evoluir.
Essa documentação, junto com índices bem planejados, ajuda o sistema a se manter organizado, auditável e eficiente por muitos anos.

=========================================================================

A tabela terá índices para otimizar consultas futuras, especialmente em campos como:
ocultarPedido (para filtros de visibilidade)
status (para filtros de status do pedido)
created_at (para filtros por data de criação)
cpf (se a busca por CPF for muito frequente)
cnpj (se a busca por CNPJ for muito frequente)
Esses índices serão adicionados conforme a necessidade de performance do sistema crescer ao longo dos anos.